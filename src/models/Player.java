package models;

public class Player extends Object {
    public Player(String textureUrl, int sizeX, int sizeY, boolean isAffectedByGravity) {
        super(textureUrl, sizeX, sizeY, isAffectedByGravity);
    }
}